---
description: Show elapsed time, current label, and tags touched in this session.
---

Run the following Bash command and relay its output verbatim:

```bash
node "${CLAUDE_PLUGIN_ROOT}/scripts/tk.mjs" status
```
