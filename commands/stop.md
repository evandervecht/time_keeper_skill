---
description: Stop the current time-keeper segment (clears the active label).
---

Run the following Bash command and relay its output verbatim:

```bash
node "${CLAUDE_PLUGIN_ROOT}/scripts/tk.mjs" stop
```
