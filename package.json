{
  "name": "time-keeper",
  "version": "0.1.7",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "for f in test/*.test.mjs; do node --test \"$f\" || exit 1; done"
  }
}
